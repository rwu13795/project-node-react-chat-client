import { memo } from "react";

function FileInput(): JSX.Element {
  return (
    <nav>
      <section>
        <h1>I am the File Input</h1>
      </section>
    </nav>
  );
}

export default memo(FileInput);
