import { memo } from "react";

function SignOut(): JSX.Element {
  return (
    <nav>
      <section>
        <h1>I am the SignOut</h1>
      </section>
    </nav>
  );
}

export default memo(SignOut);
